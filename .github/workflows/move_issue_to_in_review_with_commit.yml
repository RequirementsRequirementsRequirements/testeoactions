name: Move issue to "In review" in Projects v2

on:
  push:
    branches:
      - main

jobs:
  update-project-status:
    runs-on: ubuntu-latest

    steps:
      - name: Update Project field to "In review" for referenced issues
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN_ACTIONS_3 }}
        uses: actions/github-script@v7
        with:
          script: |
            const issuePattern = /(?:#|gh-)(\d+)/gi;
            const projectId = "PVT_kwHODArNsc4A4n6f";  // Use your Project v2 ID here if it's a Project v2
            const fieldId = "PVTSSF_lAHODArNsc4A4n6fzgtj29U";  // Field ID
            const inReviewOptionId = "df73e18b";  // Option ID for "In review"
            
            const commits = context.payload.commits;
            const issueNumbers = new Set();
            
            for (const commit of commits) {
              let match;
              while ((match = issuePattern.exec(commit.message)) !== null) {
                issueNumbers.add(Number(match[1]));
              }
            }
            
            if (issueNumbers.size === 0) {
              console.log("No referenced issues found.");
              return;
            }

            const graphqlWithAuth = github.graphql.defaults({
              headers: {
                authorization: `Bearer ${process.env.GH_TOKEN}`,
              },
            });

            for (const issueNumber of issueNumbers) {
              console.log(`🔍 Processing issue #${issueNumber}`);

              // Fetch issue data and find item in project
              const issueData = await graphqlWithAuth(`
                query($owner: String!, $repo: String!, $issue: Int!) {
                  repository(owner: $owner, name: $repo) {
                    issue(number: $issue) {
                      id
                    }
                  }
                }
              `, {
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue: issueNumber
              });

              const issueId = issueData.repository.issue.id;

              // Fetch project items and match the issue number
              const itemsData = await graphqlWithAuth(`
                query($projectId: ID!) {
                  node(id: $projectId) {
                    ... on ProjectV2 {
                      items(first: 100) {
                        nodes {
                          id
                          content {
                            ... on Issue {
                              id
                              number
                            }
                          }
                        }
                      }
                    }
                  }
                }
              `, {
                projectId
              });

              const items = itemsData.node.items.nodes;
              const matchingItem = items.find(item => item.content?.number === issueNumber);

              if (!matchingItem) {
                console.log(`❌ Issue #${issueNumber} is not in the project. Skipping.`);
                continue;
              }

              const itemId = matchingItem.id;
              console.log(`✅ Found project item ID: ${itemId}`);

              // Step 3: Update "Status" field to "In review"
              await graphqlWithAuth(`
                mutation {
                  updateProjectV2ItemFieldValue(
                    input: {
                      projectId: "${projectId}",
                      itemId: "${itemId}",
                      fieldId: "${fieldId}",
                      value: {
                        singleSelectOptionId: "${inReviewOptionId}"
                      }
                    }
                  ) {
                    projectV2Item {
                      id
                    }
                  }
                }
              `);

              console.log(`🎯 Updated issue #${issueNumber} to "In review"`);
            }
